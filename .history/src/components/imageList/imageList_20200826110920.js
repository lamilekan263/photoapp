import React from 'react';
import Images from '../ImageCard/ImageCard'

const imageList = ({ images }) =>{
    
    return(
        <div>
            imageSrc.map((imagese) =>{
                return(
                    <images />
                )
            })
        </div>
    )
}

export default imageList